import React, { useEffect, useState } from 'react';

const TABLE_HEAD = [
  { id: 'ServiceDescription', label: 'شرح خدمات' },
  { id: 'price', label: 'قیمت (ریال)' },
];

const PayForBuyPlanTable = ({ data, handleClose }) => {


  return (
<></>
  );
};

export default PayForBuyPlanTable;
